# tools/

Standalone defensive security tools and utilities — distinct from the ClawdianShield
adversary-emulation pipeline.

| File | Purpose |
|------|---------|
| `lnkscan.ps1` | PowerShell LNK file scanner — detects suspicious shortcut targets on Windows hosts (audit/detection use) |
